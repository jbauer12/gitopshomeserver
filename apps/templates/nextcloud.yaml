apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cloud
  namespace: argocd
spec:
  project: default
  destination:
    server: {{ .Values.spec.destination.server }}
    namespace: nextcloud
  source:
    repoURL: {{ .Values.spec.source.repoURL }}
    targetRevision: main
    path: opencloud/helm/charts/opencloud
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
